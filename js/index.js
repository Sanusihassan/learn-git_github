function nothing() {
    /**
     * nothing is happening here
     * just
     * a
     * bunch
     * of
     * comments
     */
}

nothing();

function scrollTop() {
    window.scroll(0, 0);
}

scrollTop();

/**
 * ststing git stash
 * trying stash options
 */